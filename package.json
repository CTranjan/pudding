{
  "name": "pudding",
  "version": "1.0.0",
  "description": "Scheduled voice announcements for Amazon Echo Dot — medication and routine reminders",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "bootstrap": "npx ts-node scripts/setup.ts",
    "upload-audio": "npx ts-node scripts/upload-audio.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1020.0",
    "@aws-sdk/client-sns": "^3.700.0",
    "@aws-sdk/client-ssm": "^3.700.0",
    "alexa-cookie2": "^5.0.3",
    "alexa-remote2": "^8.0.4",
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "aws-cdk": "^2.170.0",
    "esbuild": "^0.24.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
