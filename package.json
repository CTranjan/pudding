{
  "name": "pudding",
  "version": "1.0.0",
  "description": "Scheduled voice announcements for Amazon Echo Dot — medication and routine reminders",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "setup": "npx ts-node scripts/setup.ts"
  },
  "dependencies": {
    "alexa-remote2": "^8.0.4",
    "alexa-cookie2": "^5.0.3",
    "@aws-sdk/client-ssm": "^3.700.0",
    "@aws-sdk/client-sns": "^3.700.0",
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "ts-node": "^10.9.0",
    "aws-cdk": "^2.170.0",
    "vitest": "^2.1.0",
    "esbuild": "^0.24.0",
    "@types/node": "^22.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
