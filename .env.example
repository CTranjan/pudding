# AWS Region for CDK deployment
AWS_REGION=us-east-2

# SNS alert email — receives notifications when cookie refresh fails
ALERT_EMAIL=your-email@example.com

# Amazon account (used only by scripts/setup.ts for initial login)
AMAZON_EMAIL=your-amazon-email@example.com
